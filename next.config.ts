import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /* Not recommended because could lead to stale data
  experimental: {
    staleTimes: {
      dynamic: 30
    },
  }
    */
};

export default nextConfig;
